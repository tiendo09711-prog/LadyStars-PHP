<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;

class DatabaseSeeder extends Seeder
{
    /**
     * Seed the application's database.
     *
     * Intentionally empty: no demo accounts, sample products, or hardcoded credentials.
     * Create the first admin via Staff UI (after a one-time setup) or:
     *   php artisan tinker
     *   User::create([...]) with a strong password of your choosing.
     */
    public function run(): void
    {
        //
    }
}
