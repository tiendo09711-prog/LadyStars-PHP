# Relative /api → same origin as the page (localhost or LAN IP).
# Vite proxies /api → Laravel on this machine, so phones on Wi‑Fi work
# and MySQL stays server-side (Laravel → 127.0.0.1:3306).
VITE_API_URL=/api
