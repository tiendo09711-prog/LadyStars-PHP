# Use relative /api so any device (PC + phone on same LAN) hits Vite proxy → Laravel → MySQL.
# Do NOT set http://127.0.0.1:8000/api here — phones would call themselves, not the PC.
VITE_API_URL=/api
